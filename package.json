{
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "play": "pnpm --filter playground dev",
    "release": "bumpp package.json --commit \"chore: release v%s\" --push --tag && pnpm publish -r"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^0.26.3",
    "bumpp": "^8.2.1",
    "eslint": "^8.23.1",
    "typescript": "^4.6.4"
  },
  "eslintConfig": {
    "extends": "@antfu"
  }
}
